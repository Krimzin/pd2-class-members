PUBLIC:
	VARIABLES:
		script_reference
		script_value
		type_id
		type_name
	FUNCTIONS:
		alive
		ambient_cube_side
		children
		clipping_values
		color
		enable
		extension
		falloff_exponent
		far_range
		final_color
		inside_bs
		inside_bv
		inside_oobb
		key
		link
		local_position
		local_rotation
		m_position
		m_rotation
		multiplier
		name
		near_range
		oobb
		parent
		position
		properties
		root
		rotation
		set_ambient_cube_side
		set_clipping_values
		set_color
		set_enable
		set_extension
		set_falloff_exponent
		set_far_range
		set_final_position
		set_local_position
		set_local_rotation
		set_multiplier
		set_near_range
		set_position
		set_projection_texture
		set_properties
		set_rotation
		set_specular_multiplier
		set_spot_angle_end
		set_spot_angle_start
		set_visibility
		specular_multiplier
		spot_angle_end
		spot_angle_start
		to_local
		to_world
		tostring
		unlink
		visibility
PRIVATE:
	VARIABLES:
		__index
		__properties
		__property_writers
	FUNCTIONS:
		__eq
		__gc
		__tostring